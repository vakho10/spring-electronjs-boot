spring.application.name=spring-electronjs-boot

# stdout is reserved for the Electron handshake (PortWriter), so keep Spring's
# own console output off it. The banner is silenced here; all logging is routed
# to a file by logback-spring.xml.
spring.main.banner-mode=off
